{{#each books as |book|}}
 <!-- Temolate structure of db -->
  <h3 data-id="{{book.id}}" class="all-books"> Id: {{book.id}}</br>Title: {{book.title}}</br><small>author: {{book.author}}</small>

{{/each}}
